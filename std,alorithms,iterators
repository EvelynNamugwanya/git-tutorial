//using c++ standard library code,code reuse,effiency(fast and use less resources)
#include <iostream>
#include<vector>
#include<iterator>//points at areferance in a container //points at values in a container
#include<algorithm>
//#include<range>

using namespace std;
int main(void){
    // Your code here!
    
    vector <int>vec={4,1,8};
    vector<int>::iterator itr1 =vec.begin();//range
     vector<int>::iterator itr2 =vec.end();
     for (vector<int>::iterator itr=itr1;itr!=itr2;++itr)
    // cout<<*itr<<"";//print the content of the vector4,1,8
     //sort(itr1,itr2);//algorithm to sort
    // ranges::accumulate(vec | view::transform(multiplyBy2), 0);
     cout<<*itr*2<<"";//here i transformed by multiplying all elements of the array by 2
     
     //cout<<*itr<<"";
    
    
}
//you can also use amap istead of a vector 
